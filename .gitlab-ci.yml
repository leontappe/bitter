image: astait/flutter_linux_build:0.1.0

stages:
  - build

build:
  stage: build
  only:
    - tags
    - triggers
    - schedules
    - web
  script:
    - flutter pub get
    - flutter build linux
  artifacts:
    paths:
    - build/
